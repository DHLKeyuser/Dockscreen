# Dock Board (v5, live SharePoint)

Edit **app.js**:
- Set `DATA_URL` to your SharePoint JSON: `/sites/<YourSite>/SiteAssets/dock-board/data.json`
- Keep `PREFER_REMOTE = true` and `REFRESH_MS = 30000` (30s)

Upload folder to the same SharePoint **Site Assets/dock-board/** and open `index.html`.
